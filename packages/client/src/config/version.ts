/**
 * Single source of truth for the Atlas app version displayed in the UI.
 *
 * Bump this alongside the corresponding fields in the package.json files
 * when cutting a release. The bottom-right badge on the Home page and the
 * "About Atlas" settings panel both read from here.
 */
export const APP_VERSION = '2.0.1';
